# submissions/

Kaggle submission CSVs (or whatever the track-specific format is).
Gitignored by default — flip to tracked if we want PR-reviewed submissions.

Convention: `submissions/<track>_<experiment_id>_<YYYYMMDD>.csv`.
