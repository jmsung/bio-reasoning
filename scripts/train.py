"""Train a model from a config.

Placeholder. Loads `configs/<name>.yaml`, runs training loop, writes
checkpoint + metrics to `models/<experiment_id>/` and logs to `outputs/`.

Run: `uv run python scripts/train.py --config configs/<name>.yaml`
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("train: implement once first experiment is defined")


if __name__ == "__main__":
    main()
