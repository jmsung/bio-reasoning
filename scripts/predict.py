"""Run predictions from a trained checkpoint.

Placeholder. Loads a checkpoint from `models/<experiment_id>/` and writes
predictions to `outputs/<experiment_id>/preds/`.

Run: `uv run python scripts/predict.py --experiment <experiment_id>`
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("predict: implement after train.py lands")


if __name__ == "__main__":
    main()
