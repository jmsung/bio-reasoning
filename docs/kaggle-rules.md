# Kaggle rules

Submission format, competition rules, and gotchas for the three tracks.

**Status:** placeholder. To be filled as we read the rules pages and hit
edge cases.

This document will own:

- Per-track submission format (file layout, columns, dtypes, ID column).
- Submission limits (per day, per team) and team-merge rules.
- External data / external model rules (what is allowed, what must be
  disclosed).
- Compute / runtime rules where they apply (notebook competitions).
- Gotchas we discover while submitting (e.g., silent format requirements).

Authoritative source is the Kaggle "Rules" tab for each track — links in
[`challenge.md`](challenge.md). This page is our distilled, team-facing
working notes, not a replacement for the official rules.
